<%= simple_form_for @data_file, :html => {:multipart => true} do |f| %>
	<%= f.hidden_field :initial_input_id %>
	<%= f.input :title, label: "Título" %>
    <%= f.input :csvfile, :as => :file, :label => 'Escolha o arquivo' %>
	<%= f.button :submit, "Enviar" %>
<% end %>